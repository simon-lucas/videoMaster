package StateMachine{	import flash.utils.getTimer;	import VideoApp;	;		public class IntroState extends GenericState implements IState	{				public function IntroState(actor:*)		{			super(actor);					}				public function enter():void		{			if (debug) trace("enter FWDState. Previous state was:", previous )			actor.showStage(VideoApp.INTRO)			//actor.introSprite.init()			//actor.intro.delayShowButton()					}				public function update (tickCount:uint):void				{						//actor.popCard(1, this.unLock)		}				public function exit():void		{			if (debug) trace("exit FWDState")			actor.introSprite.deInit()			actor.hideStage(VideoApp.INTRO)					}	}}