package StateMachine{	import flash.utils.getTimer;	import VideoApp;			public class LanguageState extends GenericState implements IState	{				public function LanguageState(actor:*)		{			super(actor);					}				public function enter():void		{			if (debug) trace("enter LANG. Previous state was:", previous )			actor.showStage(VideoApp.LANG)			//actor.introSprite.init()			//actor.intro.delayShowButton()					}				public function update (tickCount:uint):void				{						//actor.popCard(1, this.unLock)		}				public function exit():void		{			if (debug) trace("exit LANGState")			actor.langSprite.deInit()			actor.hideStage(VideoApp.LANG)					}	}}