package StateMachine{	import flash.utils.getTimer;	import VideoApp;		public class TimeLineState extends GenericState implements IState	{						public function TimeLineState(actor:*)		{			super(actor);					}				public function enter():void		{			if (debug) trace("enter FWDState. Previous state was:", previous )			actor.showStage(VideoApp.TIMELINE)			actor.timelineSprite.init()					}				public function update (tickCount:uint):void				{						//actor.popCard(1, this.unLock)		}				public function exit():void		{			if (debug) trace("exit FWDState")			actor.timelineSprite.deInit()			actor.endStage(VideoApp.TIMELINE)					}	}}